{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["scenes"],
  "properties": {
    "accent_color": {
      "type": "string",
      "pattern": "^#[0-9a-fA-F]{6}$",
      "description": "Optional per-video accent override."
    },
    "scenes": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["beat_name", "scene_type"],
        "properties": {
          "beat_name": {
            "type": "string",
            "description": "The scriptwriter beat name this scene renders."
          },
          "scene_type": {
            "type": "string",
            "enum": ["stat", "diagram", "kinetic", "image", "timeline", "quiet"],
            "description": "Which Remotion scene template to use for this beat."
          },
          "stat_value": {
            "type": "string",
            "description": "The exact statistic or figure to feature for a stat scene."
          },
          "headline": {
            "type": "string",
            "maxLength": 60,
            "description": "Short on-screen text overlay, no more than ~8 words."
          }
        }
      },
      "description": "One scene plan entry per scriptwriter visual beat."
    }
  }
}